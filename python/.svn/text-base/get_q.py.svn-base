#!/usr/bin/python

from math import pi

file=open("../sim_inj/tmp/patric.dat",'r')
old = ""
for line in file:
   old = line
file.close()
line = old.split()
print float(line[11])/(2*pi), float(line[12])/(2*pi)
